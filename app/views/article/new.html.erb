<h1>Article#new</h1>

<%= form_with model: @article,url: "/article", method: :post do |form| %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= form.text_field :name, placeholder: "nom" %>
  <%= form.rich_text_area :content, placeholder: "contenu" %>
  <%= form.submit "Créer un nouvel article" %>
<% end %>
