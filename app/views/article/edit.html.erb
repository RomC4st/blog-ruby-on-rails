<h1>Article#edit</h1>
<%= form_with model:  @article,url: "/article/#{@article.id}", method: :patch do |form| %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= form.text_field :name, placeholder: "name" %>
  <%= form.rich_text_area :content %>
  <%= form.submit  "Éditer"%>
<% end %>
